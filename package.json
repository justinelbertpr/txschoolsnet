{
  "name": "txschoolsnet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "fetch": "node src/fetch.js",
    "verify": "node src/verify.js",
    "drift": "node src/drift.js",
    "build": "node src/build.js",
    "export": "node src/export.js",
    "prerender": "node src/prerender.js",
    "check:files": "node scripts/check-file-count.mjs",
    "site": "npm run verify && npm run build && npm run export && npm run prerender && npm run check:files",
    "minify": "esbuild site/style.css --minify --outfile=site/style.css --allow-overwrite && esbuild site/app.js --minify --outfile=site/app.js --allow-overwrite && esbuild site/map.js --minify --outfile=site/map.js --allow-overwrite && esbuild site/md.js --minify --outfile=site/md.js --allow-overwrite",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler deploy",
    "preview": "node scripts/serve.mjs",
    "fetch:boundaries": "node src/boundaries.js",
    "fetch:addresses": "node src/addresses.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1",
    "mapshaper": "^0.7.53",
    "vitest": "^4.1.10",
    "wrangler": "^4.34.0"
  }
}
